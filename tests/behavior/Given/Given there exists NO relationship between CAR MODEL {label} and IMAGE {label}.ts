import {Given} from "@cucumber/cucumber"

Given('there exists NO relationship between CAR MODEL {string} and IMAGE {string}',
    async function (carModelLabel: string, imageLabel: string) {
        // TODO delete relationship if it exists
    })
