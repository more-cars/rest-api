import {Given} from "@cucumber/cucumber"

Given('there exists NO relationship between BRAND {string} and IMAGE {string}',
    async function (brandLabel: string, imageLabel: string) {
        // TODO delete relationship if it exists
    })
