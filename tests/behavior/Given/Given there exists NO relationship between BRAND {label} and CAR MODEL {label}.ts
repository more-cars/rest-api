import {Given} from "@cucumber/cucumber"

Given('there exists NO relationship between BRAND {string} and CAR MODEL {string}',
    async function (brandLabel: string, carModelLabel: string) {
        // TODO delete relationship if it exists
    })
