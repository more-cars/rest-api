info:
  name: get-api-spec
  type: http

http:
  method: GET
  url: "{{baseUrl}}"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
    - expression: res.body
      operator: isJson
