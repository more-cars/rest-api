info:
  name: Get all Images (valid sort)
  type: http
  tags:
    - get
    - node
    - collection
    - sorting
    - valid

http:
  method: GET
  url: "{{baseUrl}}/images?sort_by_property=name&sort_direction=desc"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
    - expression: res.body.data
      operator: isArray
