info:
  name: Delete ›covered-by-programme-episode‹ relation (valid data)
  type: http
  tags:
    - delete
    - relation
    - valid

http:
  method: DELETE
  url: "{{baseUrl}}/car-models/{{validCarModelId}}/relationships/covered-by-programme-episode"
  body:
    type: json
    data: |-
      {
        "data": {
          "type": "covered-by-programme-episode",
          "id": "{{validProgrammeEpisodeId}}"
        }
      }

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "204"
    - expression: res.headers["cache-control"]
      operator: isNotEmpty
  scripts:
    - type: before-request
      code: |-
        const CarModels = require('./lib/node-types/CarModels.js')
        const ProgrammeEpisodes = require('./lib/node-types/ProgrammeEpisodes.js')
        await CarModels.create()
        await ProgrammeEpisodes.create()
        await CarModels.createCoveredByProgrammeEpisodeRelationship()
