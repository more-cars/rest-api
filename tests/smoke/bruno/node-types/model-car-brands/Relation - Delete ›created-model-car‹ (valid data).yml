info:
  name: Delete ›created-model-car‹ relation (valid data)
  type: http
  tags:
    - delete
    - relation
    - valid

http:
  method: DELETE
  url: "{{baseUrl}}/model-car-brands/{{validModelCarBrandId}}/relationships/created-model-car"
  body:
    type: json
    data: |-
      {
        "data": {
          "type": "created-model-car",
          "id": "{{validModelCarId}}"
        }
      }

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "204"
  scripts:
    - type: before-request
      code: |-
        const ModelCarBrands = require('./lib/node-types/ModelCarBrands.js')
        const ModelCars = require('./lib/node-types/ModelCars.js')
        await ModelCarBrands.create()
        await ModelCars.create()
        await ModelCarBrands.createCreatedModelCarRelationship()
