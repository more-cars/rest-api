info:
  name: Create ›presented-in-programme-episode‹ relation (invalid data)
  type: http
  tags:
    - create
    - relation
    - invalid

http:
  method: POST
  url: "{{baseUrl}}/car-model-variants/{{invalidCarModelVariantId}}/presented-in-programme-episode/{{invalidProgrammeEpisodeId}}"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"
    - expression: res.body
      operator: isString
    - expression: res.body
      operator: isNotEmpty
  scripts:
    - type: before-request
      code: |-
        const Ids = require('./lib/Ids.js')
        await Ids.generateInvalidId('CarModelVariant')
        await Ids.generateInvalidId('ProgrammeEpisode')
