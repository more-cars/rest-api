info:
  name: Create ›has-image‹ relation (valid data)
  type: http
  tags:
    - create
    - relation
    - valid

http:
  method: POST
  url: "{{baseUrl}}/books/{{validBookId}}/relationships/has-image"
  body:
    type: json
    data: |-
      {
        "data": {
          "type": "has-image",
          "id": "{{validImageId}}"
        }
      }

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "204"
  scripts:
    - type: before-request
      code: |-
        const Books = require('./lib/node-types/Books.js')
        const Images = require('./lib/node-types/Images.js')
        await Books.create()
        await Images.create()
    - type: after-response
      code: |-
        const Images = require('./lib/node-types/Images.js')
        await Images.delete(bru.getEnvVar('validImageId'))
