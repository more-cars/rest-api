info:
  name: Create Track Layout (invalid data)
  type: http
  tags:
    - create
    - node
    - invalid

http:
  method: POST
  url: "{{baseUrl}}/track-layouts"
  body:
    type: json
    data: |-
      {}

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "400"
    - expression: res.body
      operator: isString
