info:
  name: Create ›follows-episode‹ relation (valid data)
  type: http
  tags:
    - create
    - relation
    - valid

http:
  method: POST
  url: "{{baseUrl}}/programme-episodes/{{validProgrammeEpisodeId}}/relationships/follows-episode"
  body:
    type: json
    data: |-
      {
        "data": {
          "type": "follows-episode",
          "id": "{{validSecondProgrammeEpisodeId}}"
        }
      }

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "204"
    - expression: res.headers["cache-control"]
      operator: isNotEmpty
  scripts:
    - type: before-request
      code: |-
        const ProgrammeEpisodes = require('./lib/node-types/ProgrammeEpisodes.js')
        await ProgrammeEpisodes.create()
        await ProgrammeEpisodes.create('Second')
