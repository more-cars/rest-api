info:
  name: Create ›for-car-model-variant‹ relation (valid data)
  type: http
  tags:
    - create
    - relation
    - valid

http:
  method: POST
  url: "{{baseUrl}}/prices/{{validPriceId}}/relationships/for-car-model-variant"
  body:
    type: json
    data: |-
      {
        "data": {
          "type": "for-car-model-variant",
          "id": "{{validCarModelVariantId}}"
        }
      }

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "204"
  scripts:
    - type: before-request
      code: |-
        const Prices = require('./lib/node-types/Prices.js')
        const CarModelVariants = require('./lib/node-types/CarModelVariants.js')
        await Prices.create()
        await CarModelVariants.create()
