info:
  name: Create ›has-main-video‹ relation (valid data)
  type: http
  tags:
    - create
    - relation
    - valid

http:
  method: POST
  url: "{{baseUrl}}/racing-series/{{validRacingSeriesId}}/relationships/has-main-video"
  body:
    type: json
    data: |-
      {
        "data": {
          "type": "has-main-video",
          "id": "{{validVideoId}}"
        }
      }

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "204"
    - expression: res.headers["cache-control"]
      operator: isNotEmpty
  scripts:
    - type: before-request
      code: |-
        const RacingSeries = require('./lib/node-types/RacingSeries.js')
        const Videos = require('./lib/node-types/Videos.js')
        await RacingSeries.create()
        await Videos.create()
    - type: after-response
      code: |-
        const Videos = require('./lib/node-types/Videos.js')
        await Videos.delete(bru.getEnvVar('validVideoId'))
