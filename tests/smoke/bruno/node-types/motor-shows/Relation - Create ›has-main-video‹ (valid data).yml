info:
  name: Create ›has-main-video‹ relation (valid data)
  type: http
  tags:
    - create
    - relation
    - valid

http:
  method: POST
  url: "{{baseUrl}}/motor-shows/{{validMotorShowId}}/relationships/has-main-video"
  body:
    type: json
    data: |-
      {
        "data": {
          "type": "has-main-video",
          "id": "{{validVideoId}}"
        }
      }

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "204"
  scripts:
    - type: before-request
      code: |-
        const MotorShows = require('./lib/node-types/MotorShows.js')
        const Videos = require('./lib/node-types/Videos.js')
        await MotorShows.create()
        await Videos.create()
    - type: after-response
      code: |-
        const Videos = require('./lib/node-types/Videos.js')
        await Videos.delete(bru.getEnvVar('validVideoId'))
