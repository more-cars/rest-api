info:
  name: Get all Videos (valid sort)
  type: http
  tags:
    - get
    - node
    - collection
    - sorting
    - valid

http:
  method: GET
  url: "{{baseUrl}}/videos?sort_by_property=title&sort_direction=desc"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
    - expression: res.headers["content-type"]
      operator: contains
      value: application/vnd.api+json
    - expression: res.headers["cache-control"]
      operator: isNotEmpty
    - expression: res.body.data
      operator: isArray
