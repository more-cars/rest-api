info:
  name: Get ›by-magazine-issue‹ relation (valid data)
  type: http
  tags:
    - get
    - relation
    - valid

http:
  method: GET
  url: "{{baseUrl}}/ratings/{{validRatingId}}/by-magazine-issue"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
    - expression: res.body
      operator: isJson
    - expression: res.body.data
      operator: isJson
    - expression: res.body.data.relationship_id
      operator: gte
      value: "12000000"
    - expression: res.body.data.relationship_name
      operator: eq
      value: "by-magazine-issue"
    - expression: res.body.data.start_node
      operator: isJson
    - expression: res.body.data.start_node.node_type
      operator: eq
      value: "ratings"
    - expression: res.body.data.start_node.data.id
      operator: eq
      value: "{{validRatingId}}"
    - expression: res.body.data.partner_node
      operator: isJson
    - expression: res.body.data.partner_node.node_type
      operator: eq
      value: "magazine-issues"
    - expression: res.body.data.partner_node.data.id
      operator: eq
      value: "{{validMagazineIssueId}}"
    - expression: res.body.data.created_at
      operator: isString
    - expression: res.body.data.created_at
      operator: isNotEmpty
    - expression: res.body.data.updated_at
      operator: isString
    - expression: res.body.data.updated_at
      operator: isNotEmpty
  scripts:
    - type: before-request
      code: |-
        const Ratings = require('./lib/node-types/Ratings.js')
        const MagazineIssues = require('./lib/node-types/MagazineIssues.js')
        await Ratings.create()
        await MagazineIssues.create()
        await Ratings.createByMagazineIssueRelationship()
