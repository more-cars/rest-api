info:
  name: Create ›made-by-model-car-brand‹ relation (invalid data)
  type: http
  tags:
    - create
    - relation
    - invalid

http:
  method: POST
  url: "{{baseUrl}}/model-cars/{{invalidModelCarId}}/made-by-model-car-brand/{{invalidModelCarBrandId}}"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "404"
    - expression: res.body
      operator: isString
    - expression: res.body
      operator: isNotEmpty
  scripts:
    - type: before-request
      code: |-
        const Ids = require('./lib/Ids.js')
        await Ids.generateInvalidId('ModelCar')
        await Ids.generateInvalidId('ModelCarBrand')
