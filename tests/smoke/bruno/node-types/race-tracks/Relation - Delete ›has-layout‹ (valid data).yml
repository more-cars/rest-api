info:
  name: Delete ›has-layout‹ relation (valid data)
  type: http
  tags:
    - delete
    - relation
    - valid

http:
  method: DELETE
  url: "{{baseUrl}}/race-tracks/{{validRaceTrackId}}/relationships/has-layout"
  body:
    type: json
    data: |-
      {
        "data": {
          "type": "track-layouts",
          "id": "{{validTrackLayoutId}}"
        }
      }

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "204"
    - expression: res.headers["cache-control"]
      operator: isNotEmpty
  scripts:
    - type: before-request
      code: |-
        const RaceTracks = require('./lib/node-types/RaceTracks.js')
        const TrackLayouts = require('./lib/node-types/TrackLayouts.js')
        await RaceTracks.create()
        await TrackLayouts.create()
        await RaceTracks.createHasLayoutRelationship()
