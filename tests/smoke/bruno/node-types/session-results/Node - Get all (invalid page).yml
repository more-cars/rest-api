info:
  name: Get all Session Results (invalid page)
  type: http
  tags:
    - get
    - node
    - collection
    - pagination
    - invalid

http:
  method: GET
  url: "{{baseUrl}}/session-results?page=-2"

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "400"
    - expression: res.body
      operator: isString
    - expression: res.body
      operator: isNotEmpty
