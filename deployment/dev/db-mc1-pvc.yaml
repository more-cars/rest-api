apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-mc1-volume-dev-pvc
  namespace: dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: standard
