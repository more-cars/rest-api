name: Build Docker image

on:
  workflow_dispatch:
    inputs:
      version:
        description: Version number (e.g. 0.13.0)
        required: true
        type: string

jobs:
  build-docker-image:
    uses: ./.github/workflows/build-docker-image.yml

  push-copy-to-dockerhub:
    uses: ./.github/workflows/push-docker-image-to-dockerhub.yml
    needs: build-docker-image
