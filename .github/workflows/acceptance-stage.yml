name: Acceptance Stage

on:
  workflow_call:

jobs:
  run-smoke-tests:
    uses: ./.github/workflows/run-smoke-tests.yml
    with:
      environment: testing
      version: ${{ github.sha }}
    secrets: inherit
