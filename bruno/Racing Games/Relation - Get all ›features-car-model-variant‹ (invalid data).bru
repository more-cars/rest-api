meta {
  name: Get all ›features-car-model-variant‹ relations (invalid data)
  type: http
  tags: [
    get
    relation
    collection
    invalid
  ]
}

get {
  url: {{baseUrl}}/racing-games/{{invalidRacingGameId}}/features-car-model-variant
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  lib.tools.generateInvalidId('RacingGame')
}
