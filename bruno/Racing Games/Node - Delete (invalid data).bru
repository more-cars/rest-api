meta {
  name: Delete Racing Game (invalid data)
  type: http
  tags: [
    delete
    node
    invalid
  ]
}

delete {
  url: {{baseUrl}}/racing-games/{{invalidRacingGameId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
script:pre-request {
  lib.tools.generateInvalidId('RacingGame')
}
