meta {
  name: Delete ›features-track-layout‹ relation (valid data)
  type: http
  tags: [
    delete
    relation
    valid
  ]
}

delete {
  url: {{baseUrl}}/racing-games/{{validRacingGameId}}/features-track-layout/{{validTrackLayoutId}}
  body: none
  auth: none
}

assert {
  res.status: 204
}

script:pre-request {
  await lib.RacingGames.ensureRacingGameFeaturesTrackLayoutRelationshipExists()
}
