meta {
  name: Create ›features-car-model-variant‹ relation (invalid data)
  type: http
  tags: [
    create
    relation
    invalid
  ]
}

post {
  url: {{baseUrl}}/racing-games/{{invalidRacingGameId}}/features-car-model-variant/{{invalidCarModelVariantId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  lib.tools.generateInvalidId('RacingGame')
  lib.tools.generateInvalidId('CarModelVariant')
}
