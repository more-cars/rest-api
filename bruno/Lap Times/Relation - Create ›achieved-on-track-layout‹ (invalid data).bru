meta {
  name: Create ›achieved-on-track-layout‹ relation (invalid data)
  type: http
  tags: [
    create
    relation
    invalid
  ]
}

post {
  url: {{baseUrl}}/lap-times/{{invalidLapTimeId}}/achieved-on-track-layout/{{invalidTrackLayoutId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  lib.tools.generateInvalidId('LapTime')
  lib.tools.generateInvalidId('TrackLayout')
}
