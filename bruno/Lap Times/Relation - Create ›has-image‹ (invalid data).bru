meta {
  name: Create ›has-image‹ relation (invalid data)
  type: http
}

post {
  url: {{baseUrl}}/lap-times/{{invalidLapTimeId}}/has-image/{{invalidImageId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
