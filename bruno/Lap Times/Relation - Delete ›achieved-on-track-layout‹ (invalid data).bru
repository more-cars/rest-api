meta {
  name: Delete ›achieved-on-track-layout‹ relation (invalid data)
  type: http
}

delete {
  url: {{baseUrl}}/lap-times/{{invalidLapTimeId}}/achieved-on-track-layout/{{invalidTrackLayoutId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
