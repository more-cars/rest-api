meta {
  name: Delete ›achieved-with-car-model-variant‹ relation (invalid data)
  type: http
  tags: [
    delete
    relation
    invalid
  ]
}

delete {
  url: {{baseUrl}}/lap-times/{{invalidLapTimeId}}/achieved-with-car-model-variant/{{invalidCarModelVariantId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  const lib = require ('./lib/lib')
  lib.tools.generateInvalidId('LapTime')
  lib.tools.generateInvalidId('CarModelVariant')
}
