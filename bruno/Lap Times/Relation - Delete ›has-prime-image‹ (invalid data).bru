meta {
  name: Delete ›has-prime-image‹ relation (invalid data)
  type: http
}

delete {
  url: {{baseUrl}}/lap-times/{{invalidLapTimeId}}/has-prime-image/{{invalidImageId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
