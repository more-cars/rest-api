meta {
  name: Delete ›has-image‹ relation (invalid data)
  type: http
  tags: [
    delete
    relation
    invalid
  ]
}

delete {
  url: {{baseUrl}}/car-model-variants/{{invalidCarModelVariantId}}/has-image/{{invalidImageId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  lib.tools.generateInvalidId('CarModelVariant')
  lib.tools.generateInvalidId('Image')
}
