meta {
  name: Get all ›achieved-lap-time‹ relations (invalid data)
  type: http
  tags: [
    get
    relation
    collection
    invalid
  ]
}

get {
  url: {{baseUrl}}/car-model-variants/{{invalidCarModelVariantId}}/achieved-lap-time
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  lib.tools.generateInvalidId('CarModelVariant')
}
