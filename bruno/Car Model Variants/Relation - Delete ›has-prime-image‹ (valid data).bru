meta {
  name: Delete ›has-prime-image‹ relation (valid data)
  type: http
  tags: [
    delete
    relation
    valid
  ]
}

delete {
  url: {{baseUrl}}/car-model-variants/{{validCarModelVariantId}}/has-prime-image/{{validImageId}}
  body: none
  auth: none
}

assert {
  res.status: 204
}

script:pre-request {
  const lib = require ('./lib/lib')
  await lib.CarModelVariants.ensureValidCarModelVariantExists()
  await lib.Images.ensureValidImageExists()
  await lib.CarModelVariants.ensureCarModelVariantHasPrimeImageRelationshipExists(bru.getEnvVar('validCarModelVariantId'), bru.getEnvVar('validImageId'))
}
