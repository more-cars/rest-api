meta {
  name: Create Car Model Variant (invalid data)
  type: http
  tags: [
    create
    node
    invalid
  ]
}

post {
  url: {{baseUrl}}/car-model-variants
  body: json
  auth: none
}

body:json {
  {}
}

assert {
  res.status: 400
  res.body: isString
}
