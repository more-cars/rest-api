meta {
  name: Get all Car Model Variants (invalid sort)
  type: http
  tags: [
    get
    node
    collection
    sorting
    invalid
  ]
}

get {
  url: {{baseUrl}}/car-model-variants?sort_by_property=blubb
  body: none
  auth: none
}

assert {
  res.status: 400
  res.body: isString
}
