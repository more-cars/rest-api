meta {
  name: Get all Car Model Variants (invalid page)
  type: http
  tags: [
    get
    node
    collection
    pagination
    invalid
  ]
}

get {
  url: {{baseUrl}}/car-model-variants?page=-2
  body: none
  auth: none
}

assert {
  res.status: 400
  res.body: isString
}
