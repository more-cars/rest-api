meta {
  name: Delete Car Model Variant (invalid data)
  type: http
  tags: [
    delete
    node
    invalid
  ]
}

delete {
  url: {{baseUrl}}/car-model-variants/{{invalidCarModelVariantId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
script:pre-request {
  lib.tools.generateInvalidId('CarModelVariant')
}
