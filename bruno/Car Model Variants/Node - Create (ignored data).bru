meta {
  name: Create Car Model Variant (ignored data)
  type: http
  tags: [
    create
    node
    valid
  ]
}

post {
  url: {{baseUrl}}/car-model-variants
  body: json
  auth: none
}

body:json {
  {
    "name": "BMW M3",
    "created_at": "nope",
    "updated_at": "nonono"
  }
}

assert {
  res.status: 201
  res.body.data.created_at: neq nope
  res.body.data.updated_at: neq nonono
}
