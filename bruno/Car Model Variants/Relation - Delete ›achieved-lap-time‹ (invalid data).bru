meta {
  name: Delete ›achieved-lap-time‹ relation (invalid data)
  type: http
  tags: [
    delete
    relation
    invalid
  ]
}

delete {
  url: {{baseUrl}}/car-model-variants/{{invalidCarModelVariantId}}/achieved-lap-time/{{invalidLapTimeId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  lib.tools.generateInvalidId('CarModelVariant')
  lib.tools.generateInvalidId('LapTime')
}
