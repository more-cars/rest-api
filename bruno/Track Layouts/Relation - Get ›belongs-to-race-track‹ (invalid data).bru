meta {
  name: Get ›belongs-to-race-track‹ relation (invalid data)
  type: http
  tags: [
    get
    relation
    invalid
  ]
}

get {
  url: {{baseUrl}}/track-layouts/{{invalidTrackLayoutId}}/belongs-to-race-track
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
