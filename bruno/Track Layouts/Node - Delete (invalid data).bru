meta {
  name: Delete Track Layout (invalid data)
  type: http
}

delete {
  url: {{baseUrl}}/track-layouts/{{invalidTrackLayoutId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
