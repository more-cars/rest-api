meta {
  name: Delete ›was-used-by-racing-event‹ relation (invalid data)
  type: http
  tags: [
    delete
    relation
    invalid
  ]
}

delete {
  url: {{baseUrl}}/track-layouts/{{invalidTrackLayoutId}}/was-used-by-racing-event/{{invalidRacingEventId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  lib.tools.generateInvalidId('TrackLayout')
  lib.tools.generateInvalidId('RacingEvent')
}
