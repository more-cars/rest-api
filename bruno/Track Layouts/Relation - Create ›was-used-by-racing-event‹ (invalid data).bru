meta {
  name: Create ›was-used-by-racing-event‹ relation (invalid data)
  type: http
  tags: [
    create
    relation
    invalid
  ]
}

post {
  url: {{baseUrl}}/track-layouts/{{invalidTrackLayoutId}}/was-used-by-racing-event/{{invalidRacingEventId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
