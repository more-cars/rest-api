meta {
  name: Get all Track Layouts (invalid sort)
  type: http
  tags: [
    get
    node
    collection
    sorting
    invalid
  ]
}

get {
  url: {{baseUrl}}/track-layouts?sort_by_property=blubb
  body: none
  auth: none
}

assert {
  res.status: 400
  res.body: isString
}
