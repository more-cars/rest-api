meta {
  name: Get all ›is-featured-in-racing-game‹ relations (invalid data)
  type: http
  tags: [
    get
    relation
    collection
    invalid
  ]
}

get {
  url: {{baseUrl}}/track-layouts/{{invalidTrackLayoutId}}/is-featured-in-racing-game
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  lib.tools.generateInvalidId('TrackLayout')
}
