meta {
  name: Delete ›belongs-to-race-track‹ relation (invalid data)
  type: http
}

delete {
  url: {{baseUrl}}/track-layouts/{{invalidTrackLayoutId}}/belongs-to-race-track/{{invalidRaceTrackId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
