meta {
  name: Create ›has-lap-time‹ relation (invalid data)
  type: http
  tags: [
    create
    relation
    invalid
  ]
}

post {
  url: {{baseUrl}}/track-layouts/{{invalidTrackLayoutId}}/has-lap-time/{{invalidLapTimeId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  lib.tools.generateInvalidId('TrackLayout')
  lib.tools.generateInvalidId('LapTime')
}
