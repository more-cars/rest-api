meta {
  name: Get all Track Layouts (invalid filter)
  type: http
}

get {
  url: {{baseUrl}}/track-layouts?filter_by_property=blubb
  body: none
  auth: none
}

assert {
  res.status: 400
  res.body: isString
}
