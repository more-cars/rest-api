meta {
  name: Get all Track Layouts (invalid page)
  type: http
  tags: [
    get
    node
    collection
    pagination
    invalid
  ]
}

get {
  url: {{baseUrl}}/track-layouts?page=-2
  body: none
  auth: none
}

assert {
  res.status: 400
  res.body: isString
}
