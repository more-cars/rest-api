meta {
  name: Delete ›was-used-by-racing-event‹ relation (valid data)
  type: http
}

delete {
  url: {{baseUrl}}/track-layouts/{{validTrackLayoutId}}/was-used-by-racing-event/{{validRacingEventId}}
  body: none
  auth: none
}

assert {
  res.status: 204
}

script:pre-request {
  await lib.TrackLayouts.ensureTrackLayoutWasUsedByRacingEventRelationshipExists()
}
