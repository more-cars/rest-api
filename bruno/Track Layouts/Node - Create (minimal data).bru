meta {
  name: Create Track Layout (minimal data)
  type: http
}

post {
  url: {{baseUrl}}/track-layouts
  body: json
  auth: none
}

body:json {
  {
    "name": "GP Circuit"
  }
}

assert {
  res.status: 201
  res.body: isJson
  res.body.data.id: gte 12000000
}
