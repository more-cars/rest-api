meta {
  name: Create Track Layout (minimal data)
  type: http
  tags: [
    create
    node
    valid
  ]
}

post {
  url: {{baseUrl}}/track-layouts
  body: json
  auth: none
}

body:json {
  {
    "name": "GP Circuit"
  }
}

assert {
  res.status: 201
  res.body: isJson
  res.body.data.id: gte 12000000
}
