meta {
  name: Create Racing Series (valid data)
  type: http
  tags: [
    create
    node
    valid
  ]
}

post {
  url: {{baseUrl}}/racing-series
  body: json
  auth: none
}

body:json {
  {
    "name": "Formula 1",
    "short_name": "F1",
    "founded": 1950,
    "defunct": null,
    "organized_by": "FIA",
    "vehicle_type": "formula racing cars"
  }
}

assert {
  res.status: 201
  res.body: isJson
  res.body.data.id: gte 12000000
}

tests {
  test("both timestamps should be identical", function () {
    const data = res.getBody()
    expect(data.data.created_at).to.eql(data.data.updated_at)
  })
}
