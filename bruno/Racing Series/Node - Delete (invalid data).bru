meta {
  name: Delete Racing Series (invalid data)
  type: http
}

delete {
  url: {{baseUrl}}/racing-series/{{invalidRacingSeriesId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
