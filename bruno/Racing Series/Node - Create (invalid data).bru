meta {
  name: Create Racing Series (invalid data)
  type: http
}

post {
  url: {{baseUrl}}/racing-series
  body: json
  auth: none
}

body:json {
  {}
}

assert {
  res.status: 400
  res.body: isString
}
