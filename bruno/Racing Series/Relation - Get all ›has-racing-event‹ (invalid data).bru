meta {
  name: Get all ›has-racing-event‹ relations (invalid data)
  type: http
  tags: [
    get
    relation
    collection
    invalid
  ]
}

get {
  url: {{baseUrl}}/racing-series/{{invalidRacingSeriesId}}/has-racing-event
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
