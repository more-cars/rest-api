meta {
  name: Create Racing Series (minimal data)
  type: http
  tags: [
    create
    node
    valid
  ]
}

post {
  url: {{baseUrl}}/racing-series
  body: json
  auth: none
}

body:json {
  {
    "name": "Formula 1"
  }
}

assert {
  res.status: 201
  res.body: isJson
  res.body.data.id: gte 12000000
}
