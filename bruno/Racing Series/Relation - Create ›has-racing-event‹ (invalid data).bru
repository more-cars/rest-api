meta {
  name: Create ›has-racing-event‹ relation (invalid data)
  type: http
  tags: [
    create
    relation
    invalid
  ]
}

post {
  url: {{baseUrl}}/racing-series/{{invalidRacingSeriesId}}/has-racing-event/{{invalidRacingEventId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  lib.tools.generateInvalidId('RacingSeries')
  lib.tools.generateInvalidId('RacingEvent')
}
