meta {
  name: Get ›has-prime-image‹ relation (invalid data)
  type: http
  tags: [
    get
    relation
    invalid
  ]
}

get {
  url: {{baseUrl}}/racing-series/{{invalidRacingSeriesId}}/has-prime-image
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
