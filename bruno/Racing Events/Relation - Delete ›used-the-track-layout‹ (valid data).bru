meta {
  name: Delete ›used-the-track-layout‹ relation (valid data)
  type: http
  tags: [
    delete
    relation
    valid
  ]
}

delete {
  url: {{baseUrl}}/racing-events/{{validRacingEventId}}/used-the-track-layout/{{validTrackLayoutId}}
  body: none
  auth: none
}

assert {
  res.status: 204
}

script:pre-request {
  await lib.RacingEvents.ensureRacingEventUsedTheTrackLayoutRelationshipExists()
}
