meta {
  name: Delete ›has-racing-session‹ relation (invalid data)
  type: http
  tags: [
    delete
    relation
    invalid
  ]
}

delete {
  url: {{baseUrl}}/racing-events/{{invalidRacingEventId}}/has-racing-session/{{invalidRacingSessionId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  const lib = require ('./lib/lib')
  lib.tools.generateInvalidId('RacingEvent')
  lib.tools.generateInvalidId('RacingSession')
}
