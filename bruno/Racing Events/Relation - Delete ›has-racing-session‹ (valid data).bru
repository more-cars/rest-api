meta {
  name: Delete ›has-racing-session‹ relation (valid data)
  type: http
  tags: [
    delete
    relation
    valid
  ]
}

delete {
  url: {{baseUrl}}/racing-events/{{validRacingEventId}}/has-racing-session/{{validRacingSessionId}}
  body: none
  auth: none
}

assert {
  res.status: 204
}

script:pre-request {
  await lib.RacingEvents.ensureRacingEventHasRacingSessionRelationshipExists()
}
