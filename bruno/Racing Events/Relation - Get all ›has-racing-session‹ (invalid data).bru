meta {
  name: Get all ›has-racing-session‹ relations (invalid data)
  type: http
  tags: [
    get
    relation
    collection
    invalid
  ]
}

get {
  url: {{baseUrl}}/racing-events/{{invalidRacingEventId}}/has-racing-session
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  const lib = require ('./lib/lib')
  lib.tools.generateInvalidId('RacingEvent')
}
