meta {
  name: Delete ›has-prime-image‹ relation (valid data)
  type: http
  tags: [
    delete
    relation
    valid
  ]
}

delete {
  url: {{baseUrl}}/racing-events/{{validRacingEventId}}/has-prime-image/{{validImageId}}
  body: none
  auth: none
}

assert {
  res.status: 204
}

script:pre-request {
  const lib = require ('./lib/lib')
  await lib.RacingEvents.ensureValidRacingEventExists()
  await lib.Images.ensureValidImageExists()
  await lib.RacingEvents.ensureRacingEventHasPrimeImageRelationshipExists(bru.getEnvVar('validRacingEventId'), bru.getEnvVar('validImageId'))
}
