meta {
  name: Get ›used-the-track-layout‹ relation (invalid data)
  type: http
  tags: [
    get
    relation
    invalid
  ]
}

get {
  url: {{baseUrl}}/racing-events/{{invalidRacingEventId}}/used-the-track-layout
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
