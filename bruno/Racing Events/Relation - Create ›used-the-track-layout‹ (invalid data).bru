meta {
  name: Create ›used-the-track-layout‹ relation (invalid data)
  type: http
}

post {
  url: {{baseUrl}}/racing-events/{{invalidRacingEventId}}/used-the-track-layout/{{invalidTrackLayoutId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
