meta {
  name: Delete Racing Event (invalid data)
  type: http
  tags: [
    delete
    node
    invalid
  ]
}

delete {
  url: {{baseUrl}}/racing-events/{{invalidRacingEventId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  lib.tools.generateInvalidId('RacingEvent')
}
