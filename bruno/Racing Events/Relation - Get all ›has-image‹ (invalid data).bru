meta {
  name: Get all ›has-image‹ relations (invalid data)
  type: http
  tags: [
    get
    relation
    collection
    invalid
  ]
}

get {
  url: {{baseUrl}}/racing-events/{{invalidRacingEventId}}/has-image
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
