meta {
  name: Create Racing Event (minimal data)
  type: http
  tags: [
    create
    node
    valid
  ]
}

post {
  url: {{baseUrl}}/racing-events
  body: json
  auth: none
}

body:json {
  {
    "name": "GP Monaco 2025"
  }
}

assert {
  res.status: 201
  res.body: isJson
  res.body.data.id: gte 12000000
}
