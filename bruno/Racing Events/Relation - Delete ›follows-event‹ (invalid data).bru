meta {
  name: Delete ›follows-event‹ relation (invalid data)
  type: http
  tags: [
    delete
    relation
    invalid
  ]
}

delete {
  url: {{baseUrl}}/racing-events/{{invalidRacingEventId}}/follows-event/{{invalidSecondRacingEventId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  lib.tools.generateInvalidId('RacingEvent')
  lib.tools.generateSecondInvalidId('RacingEvent')
}
