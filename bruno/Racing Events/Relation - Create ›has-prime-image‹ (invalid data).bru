meta {
  name: Create ›has-prime-image‹ relation (invalid data)
  type: http
  tags: [
    create
    relation
    invalid
  ]
}

post {
  url: {{baseUrl}}/racing-events/{{invalidRacingEventId}}/has-prime-image/{{invalidImageId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  const lib = require ('./lib/lib')
  lib.tools.generateInvalidId('RacingEvent')
  lib.tools.generateInvalidId('Image')
}
