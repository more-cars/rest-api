meta {
  name: Delete ›follows-event‹ relation (valid data)
  type: http
  tags: [
    delete
    relation
    valid
  ]
}

delete {
  url: {{baseUrl}}/racing-events/{{validRacingEventId}}/follows-event/{{validSecondRacingEventId}}
  body: none
  auth: none
}

assert {
  res.status: 204
}

script:pre-request {
  await lib.RacingEvents.ensureRacingEventFollowsEventRelationshipExists()
}
