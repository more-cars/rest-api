meta {
  name: Create ›has-racing-session‹ relation (invalid data)
  type: http
}

post {
  url: {{baseUrl}}/racing-events/{{invalidRacingEventId}}/has-racing-session/{{invalidRacingSessionId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
