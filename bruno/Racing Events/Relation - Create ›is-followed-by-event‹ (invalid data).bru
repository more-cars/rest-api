meta {
  name: Create ›is-followed-by-event‹ relation (invalid data)
  type: http
  tags: [
    create
    relation
    invalid
  ]
}

post {
  url: {{baseUrl}}/racing-events/{{invalidRacingEventId}}/is-followed-by-event/{{invalidSecondRacingEventId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  lib.tools.generateInvalidId('RacingEvent')
  lib.tools.generateSecondInvalidId('RacingEvent')
}
