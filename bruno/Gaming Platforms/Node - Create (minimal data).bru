meta {
  name: Create Gaming Platform (minimal data)
  type: http
  tags: [
    create
    node
    valid
  ]
}

post {
  url: {{baseUrl}}/gaming-platforms
  body: json
  auth: none
}

body:json {
  {
    "name": "PlayStation 5"
  }
}

assert {
  res.status: 201
  res.body: isJson
  res.body.data.id: gte 12000000
}
