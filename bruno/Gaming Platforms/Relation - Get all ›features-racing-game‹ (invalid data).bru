meta {
  name: Get all ›features-racing-game‹ relations (invalid data)
  type: http
  tags: [
    get
    relation
    collection
    invalid
  ]
}

get {
  url: {{baseUrl}}/gaming-platforms/{{invalidGamingPlatformId}}/features-racing-game
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  lib.tools.generateInvalidId('GamingPlatform')
}
