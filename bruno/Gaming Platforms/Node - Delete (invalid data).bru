meta {
  name: Delete Gaming Platform (invalid data)
  type: http
  tags: [
    delete
    node
    invalid
  ]
}

delete {
  url: {{baseUrl}}/gaming-platforms/{{invalidGamingPlatformId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
script:pre-request {
  lib.tools.generateInvalidId('GamingPlatform')
}
