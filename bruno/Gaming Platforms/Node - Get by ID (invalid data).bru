meta {
  name: Get Gaming Platform by ID (invalid data)
  type: http
  tags: [
    get
    node
    invalid
  ]
}

get {
  url: {{baseUrl}}/gaming-platforms/{{invalidGamingPlatformId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  lib.tools.generateInvalidId('GamingPlatform')
}
