meta {
  name: Delete Race Track (valid data)
  type: http
}

delete {
  url: {{baseUrl}}/race-tracks/{{validRaceTrackId}}
  body: none
  auth: none
}

assert {
  res.status: 204
  res.body: isEmpty
}

script:pre-request {
  await lib.RaceTracks.ensureValidRaceTrackExists()
}

script:post-response {
  bru.setEnvVar("validRaceTrackId", null)
}
