meta {
  name: Delete ›has-layout‹ relation (valid data)
  type: http
}

delete {
  url: {{baseUrl}}/race-tracks/{{validRaceTrackId}}/has-layout/{{validTrackLayoutId}}
  body: none
  auth: none
}

assert {
  res.status: 204
}

script:pre-request {
  await lib.RaceTracks.ensureRaceTrackHasLayoutRelationshipExists()
}
