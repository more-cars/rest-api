meta {
  name: Delete Race Track (invalid data)
  type: http
}

delete {
  url: {{baseUrl}}/race-tracks/{{invalidRaceTrackId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
