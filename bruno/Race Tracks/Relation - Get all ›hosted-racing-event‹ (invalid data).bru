meta {
  name: Get all ›hosted-racing-event‹ relations (invalid data)
  type: http
  tags: [
    get
    relation
    collection
    invalid
  ]
}

get {
  url: {{baseUrl}}/race-tracks/{{invalidRaceTrackId}}/hosted-racing-event
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  lib.tools.generateInvalidId('RaceTrack')
}
