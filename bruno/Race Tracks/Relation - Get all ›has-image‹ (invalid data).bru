meta {
  name: Get all ›has-image‹ relations (invalid data)
  type: http
}

get {
  url: {{baseUrl}}/race-tracks/{{invalidRaceTrackId}}/has-image
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
