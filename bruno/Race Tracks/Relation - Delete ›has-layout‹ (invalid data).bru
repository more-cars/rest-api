meta {
  name: Delete ›has-layout‹ relation (invalid data)
  type: http
}

delete {
  url: {{baseUrl}}/race-tracks/{{invalidRaceTrackId}}/has-layout/{{invalidTrackLayoutId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
