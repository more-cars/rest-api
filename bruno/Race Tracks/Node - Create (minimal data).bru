meta {
  name: Create Race Track (minimal data)
  type: http
  tags: [
    create
    node
    valid
  ]
}

post {
  url: {{baseUrl}}/race-tracks
  body: json
  auth: none
}

body:json {
  {
    "name": "Lausitzring"
  }
}

assert {
  res.status: 201
  res.body: isJson
  res.body.data.id: gte 12000000
}
