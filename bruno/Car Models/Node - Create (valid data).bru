meta {
  name: Create Car Model (valid data)
  type: http
  tags: [
    create
    node
    valid
  ]
}

post {
  url: {{baseUrl}}/car-models
  body: json
  auth: none
}

body:json {
  {
      "name": "Corvette",
      "built_from": 1997,
      "built_to": 2004,
      "generation": 5,
      "internal_code": "C5",
      "total_production": 248715
    }
}

assert {
  res.status: 201
  res.body: isJson
  res.body.data.id: gte 12000000
}

tests {
  test("both timestamps should be identical", function () {
    const data = res.getBody()
    expect(data.data.created_at).to.eql(data.data.updated_at)
  })
}

