meta {
  name: Delete (invalid)
  type: http
  seq: 81
}

delete {
  url: {{baseUrl}}/car-models/{{invalidCarModelId}}
  body: none
  auth: none
}

assert {
  res.status: eq 404
  res.body: isString
}
