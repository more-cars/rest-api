meta {
  name: Get all ›has-image‹ relations (invalid data)
  type: http
}

get {
  url: {{baseUrl}}/car-models/{{invalidCarModelId}}/has-image
  body: none
  auth: none
}

assert {
  res.status: eq 404
  res.body: isString
}
