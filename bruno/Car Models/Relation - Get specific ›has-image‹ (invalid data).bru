meta {
  name: Get specific ›has-image‹ relation (invalid data)
  type: http
  tags: [
    get
    relation
    invalid
  ]
}

get {
  url: {{baseUrl}}/car-models/{{invalidCarModelId}}/has-image/{{invalidImageId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  lib.tools.generateInvalidId('CarModel')
  lib.tools.generateInvalidId('Image')
}
