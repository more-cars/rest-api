meta {
  name: Create ›belongs-to-brand‹ relation (invalid data)
  type: http
  tags: [
    create
    relation
    invalid
  ]
}

post {
  url: {{baseUrl}}/car-models/{{invalidCarModelId}}/belongs-to-brand/{{invalidBrandId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  const lib = require ('./lib/lib')
  lib.tools.generateInvalidId('CarModel')
  lib.tools.generateInvalidId('Brand')
}
