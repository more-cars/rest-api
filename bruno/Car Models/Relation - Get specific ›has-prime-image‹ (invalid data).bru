meta {
  name: Get specific ›has-prime-image‹ relation (invalid data)
  type: http
}

get {
  url: {{baseUrl}}/car-models/{{invalidCarModelId}}/has-prime-image/{{invalidImageId}}
  body: none
  auth: none
}

assert {
  res.status: eq 404
  res.body: isString
}
