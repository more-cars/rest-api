meta {
  name: Relation "has prime image" - Get (invalid)
  type: http
}

get {
  url: {{baseUrl}}/car-models/{{invalidCarModelId}}/has-prime-image
  body: none
  auth: none
}

assert {
  res.status: eq 404
  res.body: isString
}
