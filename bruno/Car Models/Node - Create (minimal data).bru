meta {
  name: Create Car Model (minimal data)
  type: http
  tags: [
    create
    node
    valid
  ]
}

post {
  url: {{baseUrl}}/car-models
  body: json
  auth: none
}

body:json {
  {
    "name": "Tigra"
  }
}

assert {
  res.status: 201
  res.body: isJson
  res.body.data.id: gte 12000000
}
