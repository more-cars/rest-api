meta {
  name: Delete ›belongs-to-brand‹ relation (invalid data)
  type: http
  tags: [
    delete
    relation
    invalid
  ]
}

delete {
  url: {{baseUrl}}/car-models/{{invalidCarModelId}}/belongs-to-brand/{{invalidBrandId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
