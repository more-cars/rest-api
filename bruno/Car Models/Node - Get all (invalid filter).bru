meta {
  name: Get all Car Models (invalid filter)
  type: http
}

get {
  url: {{baseUrl}}/car-models?filter_by_property=blubb
  body: none
  auth: none
}

assert {
  res.status: eq 400
  res.body: isString
}
