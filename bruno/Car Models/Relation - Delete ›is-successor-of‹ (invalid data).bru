meta {
  name: Delete ›is-successor-of‹ relation (invalid data)
  type: http
}

delete {
  url: {{baseUrl}}/car-models/{{invalidCarModelId}}/is-successor-of/{{invalidBrandId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
