meta {
  name: Delete ›is-successor-of‹ relation (invalid data)
  type: http
  tags: [
    delete
    relation
    invalid
  ]
}

delete {
  url: {{baseUrl}}/car-models/{{invalidCarModelId}}/is-successor-of/{{invalidSecondCarModelId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  lib.tools.generateInvalidId('CarModel')
  lib.tools.generateSecondInvalidId('CarModel')
}
