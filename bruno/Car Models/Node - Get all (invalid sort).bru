meta {
  name: Get all Car Models (invalid sort)
  type: http
  tags: [
    get
    node
    collection
    sorting
    invalid
  ]
}

get {
  url: {{baseUrl}}/car-models?sort_by_property=blubb
  body: none
  auth: none
}

assert {
  res.status: 400
  res.body: isString
}
