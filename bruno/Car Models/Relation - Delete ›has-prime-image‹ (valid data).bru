meta {
  name: Delete ›has-prime-image‹ relation (valid data)
  type: http
  tags: [
    delete
    relation
    valid
  ]
}

delete {
  url: {{baseUrl}}/car-models/{{validCarModelId}}/has-prime-image/{{validImageId}}
  body: none
  auth: none
}

assert {
  res.status: 204
}

script:pre-request {
  const lib = require ('./lib/lib')
  await lib.CarModels.ensureValidCarModelExists()
  await lib.Images.ensureValidImageExists()
  await lib.CarModels.ensureCarModelHasPrimeImageRelationshipExists(bru.getEnvVar('validCarModelId'), bru.getEnvVar('validImageId'))
}
