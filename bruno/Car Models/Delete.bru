meta {
  name: Delete
  type: http
  seq: 80
}

delete {
  url: {{baseUrl}}/car-models/{{validCarModelId}}
  body: none
  auth: none
}

assert {
  res.status: eq 204
  res.body: isEmpty
}

script:pre-request {
  await ensureValidCarModelExists()
}

script:post-response {
  bru.setEnvVar("validCarModelId", null)
}
