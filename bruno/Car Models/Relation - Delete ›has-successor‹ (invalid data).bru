meta {
  name: Delete ›has-successor‹ relation (invalid data)
  type: http
}

delete {
  url: {{baseUrl}}/car-models/{{invalidCarModelId}}/has-successor/{{invalidCarModelId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
