meta {
  name: Get all Car Models (valid filter)
  type: http
}

get {
  url: {{baseUrl}}/car-models?filter_by_property=id&filter_value=1234&filter_operator=gte
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body: isArray
}
