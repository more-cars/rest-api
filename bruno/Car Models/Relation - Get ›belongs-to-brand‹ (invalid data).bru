meta {
  name: Get ›belongs-to-brand‹ relation (invalid data)
  type: http
  tags: [
    get
    relation
    invalid
  ]
}

get {
  url: {{baseUrl}}/car-models/{{invalidCarModelId}}/belongs-to-brand
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
