meta {
  name: Create ›is-successor-of‹ relation (invalid data)
  type: http
}

post {
  url: {{baseUrl}}/car-models/{{invalidCarModelId}}/is-successor-of/{{invalidCarModelId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
