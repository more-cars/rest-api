meta {
  name: Relation "has prime image" - Create (invalid)
  type: http
}

post {
  url: {{baseUrl}}/car-models/{{invalidCarModelId}}/has-prime-image/{{invalidImageId}}
  body: none
  auth: none
}

assert {
  res.status: eq 404
  res.body: isString
}
