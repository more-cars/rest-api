meta {
  name: Delete ›has-variant‹ relation (valid data)
  type: http
  tags: [
    delete
    relation
    valid
  ]
}

delete {
  url: {{baseUrl}}/car-models/{{validCarModelId}}/has-variant/{{validCarModelVariantId}}
  body: none
  auth: none
}

assert {
  res.status: 204
}

script:pre-request {
  await lib.CarModels.ensureCarModelHasVariantRelationshipExists()
}
