meta {
  name: Delete Car Model (invalid)
  type: http
}

delete {
  url: {{baseUrl}}/car-models/{{invalidCarModelId}}
  body: none
  auth: none
}

assert {
  res.status: eq 404
  res.body: isString
}
