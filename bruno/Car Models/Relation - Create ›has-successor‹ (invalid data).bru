meta {
  name: Create ›has-successor‹ relation (invalid data)
  type: http
  tags: [
    create
    relation
    invalid
  ]
}

post {
  url: {{baseUrl}}/car-models/{{invalidCarModelId}}/has-successor/{{invalidSecondCarModelId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  lib.tools.generateInvalidId('CarModel')
  lib.tools.generateSecondInvalidId('CarModel')
}
