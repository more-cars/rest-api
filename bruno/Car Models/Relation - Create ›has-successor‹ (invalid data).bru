meta {
  name: Create ›has-successor‹ relation (invalid data)
  type: http
}

post {
  url: {{baseUrl}}/car-models/{{invalidCarModelId}}/has-successor/{{invalidBrandId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
