meta {
  name: Delete ›has-session-result‹ relation (valid data)
  type: http
  tags: [
    delete
    relation
    valid
  ]
}

delete {
  url: {{baseUrl}}/racing-sessions/{{validRacingSessionId}}/has-session-result/{{validSessionResultId}}
  body: none
  auth: none
}

assert {
  res.status: 204
}

script:pre-request {
  await lib.RacingSessions.ensureRacingSessionHasSessionResultRelationshipExists()
}
