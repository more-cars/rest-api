meta {
  name: Delete ›has-prime-image‹ relation (valid data)
  type: http
  tags: [
    delete
    relation
    valid
  ]
}

delete {
  url: {{baseUrl}}/racing-sessions/{{validRacingSessionId}}/has-prime-image/{{validImageId}}
  body: none
  auth: none
}

assert {
  res.status: 204
}

script:pre-request {
  const lib = require ('./lib/lib')
  await lib.RacingSessions.ensureValidRacingSessionExists()
  await lib.Images.ensureValidImageExists()
  await lib.RacingSessions.ensureRacingSessionHasPrimeImageRelationshipExists(bru.getEnvVar('validRacingSessionId'), bru.getEnvVar('validImageId'))
}
