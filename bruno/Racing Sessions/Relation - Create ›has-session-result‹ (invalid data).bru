meta {
  name: Create ›has-session-result‹ relation (invalid data)
  type: http
  tags: [
    create
    relation
    invalid
  ]
}

post {
  url: {{baseUrl}}/racing-sessions/{{invalidRacingSessionId}}/has-session-result/{{invalidSessionResultId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  lib.tools.generateInvalidId('RacingSession')
  lib.tools.generateInvalidId('SessionResult')
}
