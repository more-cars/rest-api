meta {
  name: Delete ›belongs-to-racing-event‹ relation (invalid data)
  type: http
  tags: [
    delete
    relation
    invalid
  ]
}

delete {
  url: {{baseUrl}}/racing-sessions/{{invalidRacingSessionId}}/belongs-to-racing-event/{{invalidRacingEventId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
