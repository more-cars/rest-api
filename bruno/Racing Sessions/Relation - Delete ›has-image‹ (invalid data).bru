meta {
  name: Delete ›has-image‹ relation (invalid data)
  type: http
}

delete {
  url: {{baseUrl}}/racing-sessions/{{invalidRacingSessionId}}/has-image/{{invalidImageId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
