meta {
  name: Delete Racing Session (invalid data)
  type: http
}

delete {
  url: {{baseUrl}}/racing-sessions/{{invalidRacingSessionId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
