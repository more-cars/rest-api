meta {
  name: Create Racing Session (minimal data)
  type: http
  tags: [
    create
    node
    valid
  ]
}

post {
  url: {{baseUrl}}/racing-sessions
  body: json
  auth: none
}

body:json {
  {
    "name": "Grand Prix"
  }
}

assert {
  res.status: 201
  res.body: isJson
  res.body.data.id: gte 12000000
}
