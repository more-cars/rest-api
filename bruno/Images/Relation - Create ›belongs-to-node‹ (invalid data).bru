meta {
  name: Create ›belongs-to-node‹ relation (invalid data)
  type: http
  tags: [
    create
    relation
    invalid
  ]
}

post {
  url: {{baseUrl}}/images/{{invalidImageId}}/belongs-to-node/{{invalidNodeId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  lib.tools.generateInvalidId('Image')
  lib.tools.generateInvalidId('Node')
}
