meta {
  name: Delete ›belongs-to-node‹ relation (invalid data)
  type: http
}

delete {
  url: {{baseUrl}}/images/{{invalidImageId}}/belongs-to-node/{{invalidCompanyId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
