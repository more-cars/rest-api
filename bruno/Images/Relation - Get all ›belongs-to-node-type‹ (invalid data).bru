meta {
  name: Get all ›belongs-to-node-type‹ relations (invalid data)
  type: http
  tags: [
    get
    relation
    collection
    invalid
  ]
}

get {
  url: {{baseUrl}}/images/{{invalidImageId}}/belongs-to-node-type
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
