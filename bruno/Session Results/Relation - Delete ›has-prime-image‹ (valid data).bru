meta {
  name: Delete ›has-prime-image‹ relation (valid data)
  type: http
  tags: [
    delete
    relation
    valid
  ]
}

delete {
  url: {{baseUrl}}/session-results/{{validSessionResultId}}/has-prime-image/{{validImageId}}
  body: none
  auth: none
}

assert {
  res.status: 204
}

script:pre-request {
  const lib = require ('./lib/lib')
  await lib.SessionResults.ensureValidSessionResultExists()
  await lib.Images.ensureValidImageExists()
  await lib.SessionResults.ensureSessionResultHasPrimeImageRelationshipExists(bru.getEnvVar('validSessionResultId'), bru.getEnvVar('validImageId'))
}
