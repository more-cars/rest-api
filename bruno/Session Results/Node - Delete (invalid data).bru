meta {
  name: Delete Session Result (invalid data)
  type: http
}

delete {
  url: {{baseUrl}}/session-results/{{invalidSessionResultId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
