meta {
  name: Delete ›achieved-with-car-model-variant‹ relation (valid data)
  type: http
  tags: [
    delete
    relation
    valid
  ]
}

delete {
  url: {{baseUrl}}/session-results/{{validSessionResultId}}/achieved-with-car-model-variant/{{validCarModelVariantId}}
  body: none
  auth: none
}

assert {
  res.status: 204
}

script:pre-request {
  const lib = require ('./lib/lib')
  await lib.SessionResults.ensureValidSessionResultExists()
  await lib.CarModelVariants.ensureValidCarModelVariantExists()
  await lib.SessionResults.ensureSessionResultAchievedWithCarModelVariantRelationshipExists(bru.getEnvVar('validSessionResultId'), bru.getEnvVar('validCarModelVariantId'))
}
