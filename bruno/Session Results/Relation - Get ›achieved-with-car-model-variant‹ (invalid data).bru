meta {
  name: Get ›achieved-with-car-model-variant‹ relation (invalid data)
  type: http
  tags: [
    get
    relation
    invalid
  ]
}

get {
  url: {{baseUrl}}/session-results/{{invalidSessionResultId}}/achieved-with-car-model-variant
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  lib.tools.generateInvalidId('SessionResult')
}
