meta {
  name: Get all Companies (valid sort)
  type: http
}

get {
  url: {{baseUrl}}/companies?sort_by_property=name&sort_direction=desc
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.data: isArray
}
