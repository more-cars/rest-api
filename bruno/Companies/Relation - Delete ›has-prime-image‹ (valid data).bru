meta {
  name: Delete ›has-prime-image‹ relation (valid data)
  type: http
  tags: [
    delete
    relation
    valid
  ]
}

delete {
  url: {{baseUrl}}/companies/{{validCompanyId}}/has-prime-image/{{validImageId}}
  body: none
  auth: none
}

assert {
  res.status: 204
}

script:pre-request {
  const lib = require ('./lib/lib')
  await lib.Companies.ensureValidCompanyExists()
  await lib.Images.ensureValidImageExists()
  await lib.Companies.ensureCompanyHasPrimeImageRelationshipExists(bru.getEnvVar('validCompanyId'), bru.getEnvVar('validImageId'))
}
