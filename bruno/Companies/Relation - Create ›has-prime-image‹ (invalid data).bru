meta {
  name: Create ›has-prime-image‹ relation (invalid data)
  type: http
  tags: [
    create
    relation
    invalid
  ]
}

post {
  url: {{baseUrl}}/companies/{{invalidCompanyId}}/has-prime-image/{{invalidImageId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  lib.tools.generateInvalidId('Company')
  lib.tools.generateInvalidId('Image')
}
