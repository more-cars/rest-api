meta {
  name: Get ›has-prime-image‹ relation (invalid data)
  type: http
}

get {
  url: {{baseUrl}}/companies/{{invalidCompanyId}}/has-prime-image
  body: none
  auth: none
}

assert {
  res.status: eq 404
  res.body: isString
}
