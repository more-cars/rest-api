meta {
  name: Get all Companies (invalid filter)
  type: http
}

get {
  url: {{baseUrl}}/companies?filter_by_property=blubb
  body: none
  auth: none
}

assert {
  res.status: eq 400
  res.body: isString
}
