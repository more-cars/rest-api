meta {
  name: Create Company (minimal data)
  type: http
}

post {
  url: {{baseUrl}}/companies
  body: json
  auth: none
}

body:json {
  {
    "name": "BMW AG"
  }
}

assert {
  res.status: eq 201
  res.body: isJson
  res.body.id: gte 12000000
}
