meta {
  name: Get all Companies (invalid sort)
  type: http
}

get {
  url: {{baseUrl}}/companies?sort_by_property=blubb
  body: none
  auth: none
}

assert {
  res.status: 400
  res.body: isString
}
