meta {
  name: Create ›has-brand‹ relation (invalid data)
  type: http
}

post {
  url: {{baseUrl}}/companies/{{invalidCompanyId}}/has-brand/{{invalidBrandId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
