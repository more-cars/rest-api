meta {
  name: Create Brand (invalid data)
  type: http
}

post {
  url: {{baseUrl}}/brands
  body: json
  auth: none
}

body:json {
  {
    "namme": "DKW"
  }
}

assert {
  res.status: eq 400
  res.body: isString
}
