meta {
  name: Create Brand (invalid data)
  type: http
  tags: [
    create
    node
    invalid
  ]
}

post {
  url: {{baseUrl}}/brands
  body: json
  auth: none
}

body:json {
  {}
}

assert {
  res.status: 400
  res.body: isString
}
