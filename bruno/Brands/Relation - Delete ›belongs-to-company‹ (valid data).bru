meta {
  name: Delete ›belongs-to-company‹ relation (valid data)
  type: http
  tags: [
    delete
    relation
    valid
  ]
}

delete {
  url: {{baseUrl}}/brands/{{validBrandId}}/belongs-to-company/{{validCompanyId}}
  body: none
  auth: none
}

assert {
  res.status: 204
}

script:pre-request {
  const lib = require ('./lib/lib')
  await lib.Brands.ensureValidBrandExists()
  await lib.Companies.ensureValidCompanyExists()
  await lib.Brands.ensureBrandBelongsToCompanyRelationshipExists(bru.getEnvVar('validBrandId'), bru.getEnvVar('validCompanyId'))
}
