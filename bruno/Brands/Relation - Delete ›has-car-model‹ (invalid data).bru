meta {
  name: Delete ›has-car-model‹ relation (invalid data)
  type: http
  tags: [
    delete
    relation
    invalid
  ]
}

delete {
  url: {{baseUrl}}/brands/{{invalidBrandId}}/has-car-model/{{invalidCarModelId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
