meta {
  name: Get all ›has-image‹ relations (invalid data)
  type: http
}

get {
  url: {{baseUrl}}/brands/{{invalidBrandId}}/has-image
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
