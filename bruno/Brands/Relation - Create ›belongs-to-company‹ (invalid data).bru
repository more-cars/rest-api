meta {
  name: Create ›belongs-to-company‹ relation (invalid data)
  type: http
  tags: [
    create
    relation
    invalid
  ]
}

post {
  url: {{baseUrl}}/brands/{{invalidBrandId}}/belongs-to-company/{{invalidCompanyId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  lib.tools.generateInvalidId('Brand')
  lib.tools.generateSecondInvalidId('Company')
}
