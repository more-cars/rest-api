meta {
  name: Create Brand (minimal data)
  type: http
}

post {
  url: {{baseUrl}}/brands
  body: json
  auth: none
}

body:json {
  {
    "name": "DKW"
  }
}

assert {
  res.status: eq 201
  res.body: isJson
  res.body.data.id: gte 12000000
}
