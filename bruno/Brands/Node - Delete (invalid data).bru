meta {
  name: Delete Brand (invalid data)
  type: http
  tags: [
    delete
    node
    invalid
  ]
}

delete {
  url: {{baseUrl}}/brands/{{invalidBrandId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}
