meta {
  name: Delete Brand (valid data)
  type: http
  tags: [
    delete
    node
    valid
  ]
}

delete {
  url: {{baseUrl}}/brands/{{validBrandId}}
  body: none
  auth: none
}

assert {
  res.status: 204
  res.body: isEmpty
}

script:pre-request {
  const lib = require ('./lib/lib')
  await lib.Brands.ensureValidBrandExists()
}

script:post-response {
  bru.setEnvVar("validBrandId", null)
}
