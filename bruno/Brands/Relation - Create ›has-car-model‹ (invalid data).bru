meta {
  name: Create ›has-car-model‹ relation (invalid data)
  type: http
  tags: [
    create
    relation
    invalid
  ]
}

post {
  url: {{baseUrl}}/brands/{{invalidBrandId}}/has-car-model/{{invalidCarModelId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  const lib = require ('./lib/lib')
  lib.tools.generateInvalidId('Brand')
  lib.tools.generateSecondInvalidId('CarModel')
}
