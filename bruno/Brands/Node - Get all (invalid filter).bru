meta {
  name: Get all Brands (invalid filter)
  type: http
  tags: [
    get
    node
    collection
    filter
    invalid
  ]
}

get {
  url: {{baseUrl}}/brands?filter_by_property=blubb
  body: none
  auth: none
}

assert {
  res.status: 400
  res.body: isString
}
