meta {
  name: Get Brand by ID (valid data)
  type: http
}

get {
  url: {{baseUrl}}/brands/{{validBrandId}}
  body: none
  auth: none
}

assert {
  res.status: 200
  res.body: isJson
  res.body.data.id: {{validBrandId}}
  res.body.data.created_at: isNotEmpty
  res.body.data.updated_at: isNotEmpty
}

script:pre-request {
  await lib.Brands.ensureValidBrandExists()
}
