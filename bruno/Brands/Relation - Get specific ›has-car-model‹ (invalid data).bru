meta {
  name: Get specific ›has-car-model‹ relation (invalid data)
  type: http
}

get {
  url: {{baseUrl}}/brands/{{invalidBrandId}}/has-car-model/{{invalidCarModelId}}
  body: none
  auth: none
}

assert {
  res.status: eq 404
  res.body: isString
}
