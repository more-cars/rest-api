meta {
  name: Get all ›has-car-model‹ relations (invalid data)
  type: http
}

get {
  url: {{baseUrl}}/brands/{{invalidBrandId}}/has-car-model
  body: none
  auth: none
}

assert {
  res.status: eq 404
  res.body: isString
}
