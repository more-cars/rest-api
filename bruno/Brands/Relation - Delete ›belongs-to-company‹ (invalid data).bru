meta {
  name: Delete ›belongs-to-company‹ relation (invalid data)
  type: http
  tags: [
    delete
    relation
    invalid
  ]
}

delete {
  url: {{baseUrl}}/brands/{{invalidBrandId}}/belongs-to-company/{{invalidCompanyId}}
  body: none
  auth: none
}

assert {
  res.status: 404
  res.body: isString
}

script:pre-request {
  const lib = require ('./lib/lib')
  lib.tools.generateInvalidId('Brand')
  lib.tools.generateSecondInvalidId('Company')
}
